<section>
  <div class="container">
    <div class="pt-5">
      <div class="row">
        <div class="col-lg-3">
          <%= render 'side_menu' %>
        </div>
        <div class="col-lg-9">
          <div class="container mt-4">
            <div class="card">
              <div class="card-body">

                <div class="border-bottom mb-3">
                  <h4>メールアドレスの変更</h4>
                </div>
                <div class="d-flex align-items-center">
                  <label class="mb-0">現在のメールアドレス</label>
                  <div class="ps-3"><%= current_user.email %></div>
                </div>
                
                <%= form_with model: @user, url: update_email_account_settings_path, local: true do |f| %> 
                  <div class="d-flex align-items-center mt-3">
                    <%= f.label :new_email, t('.email'), class: 'mb-0 me-3' %>
                    <%= f.email_field :new_email, class: 'form-control', style: "width: 50%;", placeholder: 'example@example.com' %>
                  </div>
                  <div class="pt-4 text-center">
                    <h4>確認メールが新しいメールアドレスに送信されます。</h4>
                  </div>
                  <div class="pt-4">
                    <p>・受信した確認URLの有効期限は30分です</p>
                    <p>・メールアドレスの受信拒否設定がされていないかご確認ください</p>
                  </div>
                  <div class="text-center">
                    <%= link_to 'キャンセル' , account_setting_path(current_user), class: 'btn btn-outline-secondary mt-3 me-3' %>
                    <%= f.submit '確認メールを受け取る', class: 'btn btn-info mt-3' %>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
